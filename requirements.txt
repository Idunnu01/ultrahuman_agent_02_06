Flask==2.3.3
Flask-SQLAlchemy==3.0.5
Flask-Migrate==4.0.5
Flask-CORS==4.0.0
psycopg2-binary==2.9.7
redis==5.0.1
python-dotenv==1.0.0
requests==2.31.0
twilio==8.10.0
openai==1.3.5
anthropic==0.7.7

# Statistical and ML libraries (Python 3.11 compatible versions)
numpy>=1.24.0
scipy>=1.10.0
pandas>=2.0.0
scikit-learn>=1.3.0
statsmodels>=0.14.0

# Optional packages (install what works)
python-dateutil==2.8.2
pytz==2023.3

# Remove problematic packages for now:
# pingouin==0.5.3          # May have dependency issues
# minepy==1.2.6            # Compilation issues
# pywt==1.4.1              # Not compatible with Python 3.11
# ruptures==1.1.8          # May have issues
# hmmlearn==0.3.0          # May have issues
# tensorflow==2.13.0       # Heavy and may have issues
# sentence-transformers==2.2.2  # Heavy dependency
# mlxtend==0.22.0          # May have dependency issues
# qdrant-client==1.6.4     # Optional for now